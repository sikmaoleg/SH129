<?php
/**
 * Настройки подключения.
 * Скопируйте этот файл в config/config.php и впишите свои данные.
 * config.php в репозиторий не попадает — там пароли.
 */

return [
    // --- База данных (данные берутся в панели управления хостинга) ---
    'db' => [
        'host'     => 'localhost',
        'name'     => 'имя_базы',
        'user'     => 'пользователь_базы',
        'password' => 'пароль_базы',
        'charset'  => 'utf8mb4',
    ],

    // --- Сайт ---
    // Базовый URL без слэша на конце. Пустая строка = сайт в корне домена.
    'base_url'    => '',

    // Часовой пояс для дат
    'timezone'    => 'Europe/Moscow',

    // Показывать подробности ошибок. На боевом сайте — false.
    'debug'       => false,

    // Ключ доступа к панели разработчика (кроме роли dev).
    // Смените на свой перед публикацией.
    'dev_key'     => 'change-me-please',
];
